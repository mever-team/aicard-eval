from evaluation import __version__
from setuptools import setup, find_packages

setup(
    name="aicar-eval",
    version=__version__,
    packages=find_packages(),
    install_requires=[],
)
